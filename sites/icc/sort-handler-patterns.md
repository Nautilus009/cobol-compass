# ICC Sort Handler Patterns

- Preserve sort key definitions and ordering unless the change request explicitly modifies them.
- Review temporary file locations and permissions.
- Check input and output procedures.
- Include duplicate keys, empty input, and large input tests when relevant.

