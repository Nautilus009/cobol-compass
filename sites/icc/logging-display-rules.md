# ICC Logging and DISPLAY Rules

- Preserve existing DISPLAY formats where operational tools may parse them.
- Do not add noisy diagnostic output to production paths without approval.
- Keep error messages clear enough for support triage.
- Include status values and key identifiers when nearby code does so.
- Remove temporary debugging output before final delivery.

