# Linux Signals and Core Dumps

When diagnosing crashes:

- Capture signal name or number.
- Check whether a core dump was produced.
- Record runtime command, working directory, and environment.
- Review shared library versions.
- Preserve logs and input data needed for reproduction.
- Distinguish runtime abends from application return-code failures.

